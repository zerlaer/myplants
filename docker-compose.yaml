version: '3.8'

services:
  myplants:
    image: myplants
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myplants
    ports:
      - "8020:8020"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    networks:
      - app
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8020/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  app:
    external: true

